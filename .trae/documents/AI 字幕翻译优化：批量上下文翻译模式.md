收到，我们将采用**分批次批量翻译**策略，默认批次大小为 **200 行**，并且这个参数将是可配置的。

### **优化后的方案**

#### **1. 核心逻辑 (`ai_translator.py`)**
*   **分块逻辑**: 将解析后的字幕列表按 `batch_size`（默认 200）进行切分。
*   **Prompt 构建**: 将每一批次的字幕重新组合成 SRT 格式文本，作为 Prompt 发送给 AI。
*   **流式处理**: 使用 `stream=True` 调用 API，实时接收返回的文本块，尝试解析并更新 UI。

#### **2. 配置管理 (`SettingsTab`)**
*   在 AI 设置区域增加一个输入框：**“每批次行数”** (Batch Size)，默认值 `200`。

#### **3. 进度与预览 (`TranslationThread` & `TranslationDialog`)**
*   **进度**: 现在进度将以“批次”或“已处理行数”为单位更新。
*   **预览**: 当一个批次翻译完成后，将其中的内容解析出来，追加到预览窗口。由于是流式返回，我们可以尝试实时显示原始返回文本，或者等该批次完成后统一格式化显示。为了体验更好，我们将显示 AI 返回的**原始文本流**到预览窗口，这样用户能看到即时的生成过程。

### **实施步骤**

1.  **修改 `ai_translator.py`**:
    *   重构 `translate_text` 为 `translate_batch_stream`，支持流式输出。
    *   重构 `TranslationThread`，实现分批循环逻辑。
2.  **修改 `settings_tab.py`**:
    *   添加 `ai_batch_size` 配置项。
3.  **修改 `ui.py`**:
    *   在调用 `TranslationDialog` 时传入 `batch_size`。
4.  **修改 `translation_dialog.py`**:
    *   适配新的流式预览逻辑（直接显示 AI 返回的文本流，或者尝试解析高亮）。

准备好开始代码修改了吗？